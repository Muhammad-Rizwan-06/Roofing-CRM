import React from "react";
import LeadsPipeline from "./LeadsPipeline";

const Opportunities = () => {
  // ✅ Use the real pipeline (localStorage leads) instead of mockData
  return <LeadsPipeline />;
};

export default Opportunities;